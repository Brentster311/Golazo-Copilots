# FRC-007 Program Manager Notes

## Decisions
- Deliver direct connector path as additive capability while preserving fixture connectors.
- Keep planner sync contract stable for downstream UI/API compatibility.
- Sequence: connector abstractions -> direct connector tests -> planner integration verification.
