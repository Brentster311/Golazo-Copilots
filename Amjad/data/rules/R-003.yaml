rule_id: R-003
status: CONFIRMED
sources:
- INC-001
conditions:
  logic: AND
  items:
  - noun: AppRegistration
    instance: $app
    property: adminConsent
    operator: ==
    value: required
then:
  kind: CHANGE_STATE
  description: AppRegistration.adminConsent => granted
else:
  kind: RULED_OUT
  description: AppRegistration.adminConsent
